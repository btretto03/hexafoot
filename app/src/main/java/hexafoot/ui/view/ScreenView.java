package hexafoot.ui.view;

import javafx.scene.Parent;

/** Contrato mínimo para telas que podem ser instaladas pelo navegador do jogo. */
public interface ScreenView {
    Parent getRoot();
}
